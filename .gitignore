# local full datasets — too large for GitHub Pages, regenerate with scripts/import.py
data/
packages/python/motd/__pycache__/
*.pyc

# generator cache (local only — today.json in repo root is the committed version)
generator/cache.json
